I’ve fixed the tooltip display issue, and it’s working great. Now, I want to update the `otherNetworks` feature in all wallet JSONs: change any value of `"no"` or `"does_not_apply"` to `"API"`. This should be reflected in the comparison table and tooltips, with updated translations in both English and Spanish in `en.json` and `es.json`.

Please:
1. **Suggest Data Changes**:
   - Provide a script or instructions to update all wallet JSON files to:
     - Replace `"no"` or `"does_not_apply"` with `"API"` for the `otherNetworks` feature.
     - Leave other values (e.g., `"yes"`, `"custom"`, or objects like `{ "value": "custom", "customText": "..." }`) unchanged.
   - Example transformation:
     ```json
     // Before
     "features": {
       "otherNetworks": "no",
       "platform": ["android"]
     }
     // or
     "features": {
       "otherNetworks": "does_not_apply",
       "platform": ["android"]
     }
     // After
     "features": {
       "otherNetworks": "API",
       "platform": ["android"]
     }
Update Translations:
Update en.json with an English translation for "API":
json

Collapse

Wrap

Copy
"featureStatus": {
  "values": {
    "API": { "label": "API", "title": "Uses an API for network support" }
  }
}
Update es.json with a Spanish translation for "API":
json

Collapse

Wrap

Copy
"featureStatus": {
  "values": {
    "API": { "label": "API", "title": "Usa una API para soporte de red" }
  }
}
Ensure these translations apply to otherNetworks: "API" in the table via FeatureTooltip.
Verify Rendering:
Confirm ComparisonTable.tsx renders otherNetworks: "API" correctly (likely as a text pill unless you suggest an icon).
Check that FeatureTooltip displays "Uses an API for network support" (English) or "Usa una API para soporte de red" (Spanish) based on the language setting.
Test:
Verify that a wallet with otherNetworks previously set to "no" or "does_not_apply" now shows "API" with the correct tooltip in both English and Spanish modes.
Don’t modify unrelated functionality unless necessary. If "Bitcoin Network" is not otherNetworks, let me know the correct feature name.
Let me know the script/instructions for updating the wallet JSONs, confirm the en.json and es.json updates match the exact translations I provided, and verify that otherNetworks: "API" renders correctly with the right tooltips in both languages!